#include <stdlib.h>
#include <stdio.h>

/**
 * mul - multiplies two intergers together
 * @i: first par
 * @j: second par
 * Return: multiplaction of numbers
 */
int mul(int i, int j)
{
	return (i * j);
}
